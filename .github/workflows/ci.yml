name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install

      - name: Type-check
        run: npm run typecheck

      - name: Build
        run: npm run build

      - name: Prepare DirectAdmin deployment bundle
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
          rm -rf deploy
          mkdir -p deploy/database
          cp server.js deploy/server.js
          cp package.json deploy/package.json
          cp VERSION deploy/VERSION
          cp DEPLOYMENT-v0.3.md deploy/DEPLOYMENT-v0.3.md
          cp database/003_community_foundation.sql deploy/database/003_community_foundation.sql
          cp -a .next deploy/.next
          rm -rf deploy/.next/cache deploy/.next/types deploy/.next/diagnostics
          if [ -d public ]; then cp -a public deploy/public; fi

      - name: Upload DirectAdmin deployment bundle
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        uses: actions/upload-artifact@v4
        with:
          name: gamenight-tools-v0.3.0-directadmin
          path: deploy/
          include-hidden-files: true
          retention-days: 14
